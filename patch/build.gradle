plugins {
    id 'java'
}

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    // 向外直接传递hotfix-base这个库
    implementation project(path:':hotfix-base')
    implementation 'androidx.annotation:annotation:1.3.0'
}